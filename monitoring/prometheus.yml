global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Add per-agent /api/metrics targets after services expose Prometheus endpoints.
